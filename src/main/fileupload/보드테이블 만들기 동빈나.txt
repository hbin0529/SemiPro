보드 테이블 

CREATE TABLE PASS_BOARD(
BBSID NUMBER PRIMARY KEY,
BBSTITLE VARCHAR2(50),
USERID VARCHAR2(20),
BBSDATE DATE, 
BBSCONTENT VARCHAR2(2048),
BBSAVAILABLE NUMBER
);

CREATE TABLE PASS_BOARD2(
PB_NUM NUMBER,
PB_NAME VARCHAR2(20),
PB_TITLE VARCHAR2(50),
PB_CONTENT VARCHAR2(4000),
PB_DATE DATE,
MEM_ID VARCHAR2(30) PRIMARY KEY
);